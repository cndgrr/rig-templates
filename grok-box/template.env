# grok-box — the xAI Grok agent tenant (ported byte-equivalent from rig's
# case arms in rig#110). Grok's CLI is a self-contained binary: no node.
USER="grok"
CONTEXT_PATH=".grok/AGENTS.md"
CLI_NAME="grok"
CLI_SRC="~/.grok/bin/grok"
PATH_LINE="export PATH="$HOME/.grok/bin:$PATH""
NEEDS_NODE="no"
