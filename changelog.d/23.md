### Added

- Every agent tenant declares `APT_EXTRAS="cron gh jq"` — crew's box-side
  prerequisites — with vendor packages layered on top (#23).
- `test/agent-apt-extras.sh` holds that base in CI: dropping one reds, adding
  a vendor package does not. No definition installs crew's duty engine (#23).
- `drill/drill.sh --crew-member` runs crew's installer on a converged tenant
  and proves cron **armed**, not merely installed (#23).
